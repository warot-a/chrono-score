import { WorldCupShell } from '@/components/WorldCup/WorldCupShell';

export default function WorldCupLayout({ children }: { children: React.ReactNode }) {
  return <WorldCupShell>{children}</WorldCupShell>;
}
