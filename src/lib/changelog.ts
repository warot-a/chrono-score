import pkg from '../../package.json';

export const CHANGELOG = {
  version: pkg.version,
  items: ['Fixed incorrect match information in the schedule view.', 'Temporarily hidden the bracket view.'],
};
